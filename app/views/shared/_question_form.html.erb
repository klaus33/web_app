<%= form_for(@quest) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Escriba una nueva Pregunta..." %>
  </div>
  <%= f.submit "Preguntar", class: "btn btn-large btn-primary" %>
<% end %>
